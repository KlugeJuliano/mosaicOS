local L4 = require("L4");

local loader = L4.default_loader;
local cmd = loader:new_channel();

loader:start({
  log = { "mosaic-init", "cyan" },
  caps = { svr = cmd },
}, "rom/mosaic-init rom/mosaic-init.manifest");

L4.server_loop(cmd);
